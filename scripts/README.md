# mflash Scripts

Utility scripts for validating and migrating mflash files.

## Validate examples

```bash
python scripts/validate_examples.py

This checks JSON syntax for schema and example files.

Future scripts will include schema validation, v1-to-v2 migration, and deck normalization.
